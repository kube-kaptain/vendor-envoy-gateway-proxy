# Source: gateway-helm/templates/infra-manager-rbac.yaml
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: vendor-envoy-gateway-infra-manager
  namespace: '${Environment}'
  labels:
    app: vendor-envoy-gateway-infra-manager
    app.kubernetes.io/name: vendor-envoy-gateway-infra-manager
    app.kubernetes.io/version: "${Version}"
    app.kubernetes.io/managed-by: Kaptain
    app.kubernetes.io/part-of: ${ProductName}
    kaptain.org/environment: ${Environment}
    kaptain.org/product: ${ProductName}
    kaptain.org/project-name: ${ProjectName}
    kaptain.org/version: "${Version}"
    kaptain.org/owner: kube-kaptain
  annotations:
    kaptain.org/upstream-source: kube-kaptain/vendor-envoy-gateway:1.7.1.8
    kaptain.org/source-repository: kube-kaptain/vendor-envoy-gateway-proxy
    kaptain.org/generated-by: Generated by .github/bin/pull-vendor-envoy-gateway.bash
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: 'vendor-envoy-gateway-infra-manager'
subjects:
  - kind: ServiceAccount
    name: 'envoy-gateway'
    namespace: '${RunPlatform}'
